{% extends 'base.html.twig' %}

{% block title %}{{ 'title.reset_password' | trans }}{% endblock %}

{% block body %}   
<div class="account-pages w-100 mt-5 mb-5">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8 col-lg-6 col-xl-5">
                <div class="card">
                    <div class="card-header">{{ 'message.emailed_reset_link' | trans }}</div>
                    <div class="card-body">
                        <a href="{{ path('app_login') }}" class="align-self-center">{{ 'title.login'|trans }}</a>
                    </div>
                </div>
            </div>
        </div> 
    </div>
    <!-- end container -->
</div>                                     
{% endblock %}