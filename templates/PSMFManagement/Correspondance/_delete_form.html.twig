<form method="post" action="{{ path('template_management_correspondance_delete', {'id': correspondance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ correspondance.id) }}">
    <button class="btn">Delete</button>
</form>
